git clone git@github.com:willserpa86/gerenciador-de-tarefas.git cd gerenciador-de-tarefas echo "# Gerenciador de Tarefas" 
README.md git add README.md git commit -m "Initial commit" git branch -M main git push -u origin main
